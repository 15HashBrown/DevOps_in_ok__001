version: 0.0
os: linux
files:
  - source: /
    destination: /home/
hooks:
  ApplicationStop:
    - location: stop_server.sh
      timeout: 300
  AfterInstall:
    - location: afterinstall.sh
      timeout: 300
  ApplicationStart:
    - location: applicationstart.sh
      timeout: 300
     
